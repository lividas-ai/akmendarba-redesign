/**
 * Build-time client selection.
 *
 * Each generated client repository replaces this file and the adjacent client
 * package. The presentation layer imports only from this stable boundary.
 */
export { granitDecorSiteConfig as activeSiteConfig } from "@/client/granit-decor/site-config";
